# Frontend

1. cd frontend
2. npm install
3. npm run dev
- App runs at http://localhost:5173

If there is an ESM plugin error on Windows: ensure Node 18+, delete node_modules and package-lock.json and reinstall.
